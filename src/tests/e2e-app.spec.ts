import {describe, expect, it } from "vitest";

describe("E2E - Application Render", ()=> {
       it("should return 200 OK and render homepage HTML", async ()=> {
              expect(3).toBe(3);
       });
});