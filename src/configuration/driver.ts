import { driver } from "driver.js";
import { DriverOptionsType } from "driverjs-react";

export const driverOptions:DriverOptionsType = {
       showProgress: false, // default
       allowClose: true,
};
